# Backend API

Node + Express + TypeScript + Sequelize (SQLite)

## Setup

1. cd backend
2. npm install
3. npm run dev

## Endpoints

GET /tests
GET /results/:testId/aggregate
GET /results/:testId/histogram
POST /import
