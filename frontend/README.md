# Frontend UI

React + Vite + TypeScript frontend for the Markr task.

## Setup

1. cd frontend
2. npm install
3. npm run dev

The app expects the backend API at `/api/users`. Use a proxy or run the backend on the same origin.
